---
title: Installation
description: "Install the CPM TypeScript SDK"
---

## Requirements

- Node.js 18.0.0 or higher
- npm, yarn, or pnpm

## Installation

<Tabs>
  <Tab title="npm">
    ```bash
    npm install cpm
    ```
  </Tab>
  <Tab title="yarn">
    ```bash
    yarn add cpm
    ```
  </Tab>
  <Tab title="pnpm">
    ```bash
    pnpm add cpm
    ```
  </Tab>
  <Tab title="Global">
    ```bash
    npm install -g cpm
    ```
  </Tab>
</Tabs>

## Verify Installation

```bash
# Check version
cpm --version

# Show help
cpm --help
```

## Quick Start

```bash
# Initialize a new mono repo
cpm init my-monorepo
cd my-monorepo

# Create a project
cpm create project web-app

# Create a shared skill
cpm create skill logging

# Add skill to project
cpm add skill:logging --to web-app

# View project
cpm get web-app
```

## Programmatic Usage

```typescript
import { loadCpmConfig, resolveProject } from "cpm";

const config = await loadCpmConfig("/path/to/monorepo");
console.log(`Repo: ${config.name}`);

const project = await resolveProject("my-project", "/path/to/monorepo");
console.log(`Project: ${project.name}`);
```

## TypeScript Support

The SDK is written in TypeScript and provides full type definitions:

```typescript
import type {
  CpmConfig,
  ProjectConfig,
  ComponentMetadata,
  ResolvedProject,
} from "cpm";
```

## Next Steps

<CardGroup cols={2}>
  <Card title="Usage Guide" icon="book" href="/sdks/typescript/usage">
    Learn how to use the SDK
  </Card>
  <Card title="API Reference" icon="code" href="/sdks/typescript/api-reference">
    Complete API documentation
  </Card>
  <Card title="CLI Reference" icon="terminal" href="/cli/overview">
    Complete CLI command reference
  </Card>
  <Card title="Examples" icon="folder-open" href="/examples/basic-monorepo">
    Example mono repo setups
  </Card>
</CardGroup>
